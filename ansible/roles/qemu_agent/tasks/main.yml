- name: Instalar qemu-guest-agent
  apt:
    name: qemu-guest-agent
    state: present
    update_cache: yes

- name: Habilitar qemu-guest-agent
  systemd:
    name: qemu-guest-agent
    enabled: yes
    state: started
